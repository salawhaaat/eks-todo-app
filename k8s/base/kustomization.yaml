# Base layer - orchestrates all infrastructure
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - infrastructure/

labels:
  - pairs:
      managed-by: kustomize
      project: todo-app
    includeSelectors: false
